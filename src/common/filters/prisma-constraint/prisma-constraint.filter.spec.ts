import { PrismaConstraintFilter } from './prisma-constraint.filter';

describe('PrismaConstraintFilter', () => {
  it('should be defined', () => {
    expect(new PrismaConstraintFilter()).toBeDefined();
  });
});
